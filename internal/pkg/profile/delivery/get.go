package profiledelivery

import (
	"codex/internal/pkg/domain"
	// "codex/internal/pkg/utils/cast"

	// "encoding/json"
	"github.com/gorilla/mux"
	"net/http"
	"strconv"
)

func (handler *ProfileHandler) getProfile(w http.ResponseWriter, r *http.Request) {
	// params := mux.Vars(r)
	// movId, err := strconv.ParseUint(params["id"], 10, 64)
	// if err != nil {
	// 	http.Error(w, domain.Err.ErrObj.ParseId.Error(), http.StatusBadRequest)
	// 	return
	// }


	// w.WriteHeader(http.StatusOK)
	// w.Write(out)
}

func (handler *ProfileHandler) getBookmarks(w http.ResponseWriter, r *http.Request) {
	// params := mux.Vars(r)
	// movId, err := strconv.ParseUint(params["id"], 10, 64)
	// if err != nil {
	// 	http.Error(w, domain.Err.ErrObj.ParseId.Error(), http.StatusBadRequest)
	// 	return
	// }


	// w.WriteHeader(http.StatusOK)
	// w.Write(out)
}

func (handler *ProfileHandler) getReviews(w http.ResponseWriter, r *http.Request) {
	// params := mux.Vars(r)
	// movId, err := strconv.ParseUint(params["id"], 10, 64)
	// if err != nil {
	// 	http.Error(w, domain.Err.ErrObj.ParseId.Error(), http.StatusBadRequest)
	// 	return
	// }


	// w.WriteHeader(http.StatusOK)
	// w.Write(out)
}
